# Streamlit Secrets Configuration
# Add your secrets here for deployment

# Ollama Configuration
# Note: For Streamlit Cloud, you'll need to configure Ollama to be accessible
# via a public endpoint or use an alternative LLM service

[ollama]
host = "http://localhost:11434"
model = "tinyllama"

# Alternative: Use OpenAI or other cloud LLM services
# [openai]
# api_key = "your-api-key-here"
